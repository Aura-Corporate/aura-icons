# aura_solar_icons_example

A minimal example app demonstrating the `aura_solar_icons` package: a grid
per style (Outline, Linear, Bold, Broken, BoldDuotone, LineDuotone).

Run with `flutter run` from this directory.
